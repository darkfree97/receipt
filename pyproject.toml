[project]
name = "receipt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.4",
    "gunicorn>=25.3.0",
    "uvicorn[standard,standart]>=0.46.0",
]
